import { CSSResult, LitElement, TemplateResult } from 'lit';
export declare enum Device {
    iPad = "ipad",
    iPhone = "iphone",
    macBook = "macbook"
}
export declare class ResponsiveDevice extends LitElement {
    device: Device;
    render(): TemplateResult;
    private get deviceIcon();
    static get styles(): CSSResult;
}
