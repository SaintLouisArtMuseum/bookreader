import { CSSResult, LitElement, TemplateResult } from 'lit-element';
export declare class AppRoot extends LitElement {
    private searchService;
    private searchInput;
    private metadataInput;
    private searchResponse?;
    private metadataResponse?;
    private get searchResults();
    /** @inheritdoc */
    render(): TemplateResult;
    private get resultsTemplate();
    private get metadataTemplate();
    getMetadata(e: Event): Promise<void>;
    search(e: Event): Promise<void>;
    static get styles(): CSSResult;
}
