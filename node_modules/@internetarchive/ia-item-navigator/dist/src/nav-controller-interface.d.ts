import { LitElement } from "lit-element";
export interface NavControllerInterface extends LitElement {
    baseHost: string;
    signedIn: boolean;
}
export interface BookNavInterface extends NavControllerInterface {
    baseHost: string;
    signedIn: boolean;
}
