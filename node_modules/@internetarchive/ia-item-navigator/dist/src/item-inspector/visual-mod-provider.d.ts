import { TemplateResult } from 'lit-element';
import '@internetarchive/ia-sharing-options';
import '@internetarchive/icon-visual-adjustment/icon-visual-adjustment';
import { IntMenuProvider, IntProviderArgs } from '../interfaces/menu-interfaces';
export declare class VisualModsProvider implements IntMenuProvider {
    item: any;
    baseHost: string;
    icon: TemplateResult;
    id: string;
    label: string;
    component: TemplateResult | null;
    subPrefix: string;
    encodedSubPrefix: string;
    updated: any;
    constructor(providerArgs: IntProviderArgs);
    openModal(): void;
    closeModal(): void;
    toggleFullscreen(): void;
}
