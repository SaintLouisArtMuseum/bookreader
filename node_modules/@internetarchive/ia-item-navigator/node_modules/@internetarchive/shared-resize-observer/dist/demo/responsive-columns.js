import { __decorate } from "tslib";
import { css, customElement, html, LitElement } from "lit-element";
let ResponsiveColumns = class ResponsiveColumns extends LitElement {
    render() {
        return html `
      <div class="container">
        <main>
          <slot name="main"></slot>
        </main>
        <section id="sidebar">
          <slot name="sidebar"></slot>
        </section>
      </div>
    `;
    }
    static get styles() {
        return css `
      .container {
        display: flex;
        width: 100%;
        height: 100%;
      }

      main {
        background-color: #eee;
        width: 75vw;
        text-align: center;
        font-size: 2rem;
      }

      #sidebar {
        background-color: #aaa;
        width: 35vw;
        text-align: center;
        font-size: 2rem;
      }
    `;
    }
};
ResponsiveColumns = __decorate([
    customElement('responsive-column')
], ResponsiveColumns);
export { ResponsiveColumns };
//# sourceMappingURL=responsive-columns.js.map